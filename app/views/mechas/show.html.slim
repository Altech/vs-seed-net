.data-title
  h1
    == @mecha.full_name

.border-top-of-stats.sp
.stats
  #chart
  #basic-data.pc
    h3 基本性能
    img src="/assets/stf.png"
      .read-more
        a href="http://www8.atwiki.jp/destiny/pages/686.html" target="_blank" [wiki]
  div style="clear: both"
.border-bottom-of-stats.sp

.filter-by-selection style='clear: both'
  | ◆動画を絞る：
  == select_tag 'foo', options_for_select(['全ての僚機', 0] + Player.all.map{|p| [p.name,p.id]})

== render partial: 'shared/thumbnails', locals: {videos: @videos.sort}

javascript:
  $(function () {
      $('#chart').highcharts({
          colors: ["#3366CC", "#DC3912", "#FF9900", "#109618", "#990099", "#0099C6", "#DD4477",
		"#66AA00", "#B82E2E"],
          chart: {
              plotBackgroundColor: null,
              plotBorderWidth: null,
              plotShadow: false
          },
          title: null,
          tooltip: {
      	    pointFormat: '{series.name}: <b>{point.percentage:.1f}%</b>'
          },
          plotOptions: {
              pie: {
                  allowPointSelect: true,
                  cursor: 'pointer',
                  dataLabels: {
                      enabled: true,
                      format: '{point.name}',
                      style: {
                          color: (Highcharts.theme && Highcharts.theme.contrastTextColor) || 'black'
                      }
                  }
              }
          },
          series: [{
              type: 'pie',
              name: 'Browser share',
              data: [
                  ['アカツキ（オオワシ）',   45.0],
                  ['ストライクフリーダム',       26.8],
                  ['ムラサメ（虎）', 12.8],
                  ['偵察型ジン',    8.5],
                  ['ストライクルージュ',     6.2],
                  ['その他',   0.7]
              ]
          }]
      });
  });
